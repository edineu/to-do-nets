import { createGlobalStyles } from "styled-components";

const GlobalStyles = createGlobalStyles`
    * {
        box-sizing: border-box;
    }
    body {
        font-size: 10px
        font-family: 'Hind', san-sarif;
    }
`;

export default GlobalStyles;
